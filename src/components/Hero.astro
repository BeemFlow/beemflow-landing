<section class="hero">
  <div class="container">
    <div data-fade data-fade-delay="0.1">
      <p class="hero__eyebrow label-mono">For funded software, SaaS &amp; AI founders (pre&#8209;seed to Series&nbsp;A)</p>
      <h1 class="hero__heading">
        Ship a production&#8209;ready v1.<br />
        <span class="text-accent">6&#8209;8&nbsp;weeks, not quarters.</span>
      </h1>
    </div>
    <div data-fade data-fade-delay="0.2">
      <p class="hero__body">
        We plug in as your product, engineering, and design team to take you
        from fuzzy thesis to a live, demoable v1 that's ready for your first
        real users. You bring the users; we make sure the product, onboarding,
        and analytics are ready for them.
      </p>
      <p class="hero__proof label-mono">
        10+ years building together &middot; 15+ products shipped &middot; VC&#8209;backed company founded &amp; exited &middot; Launch&#8209;date guarantee
      </p>
    </div>
    <div class="hero__actions" data-fade data-fade-delay="0.3">
      <a href="#contact" class="btn-primary">Book a 30-minute fit call &rarr;</a>
      <a href="#work" class="hero__link">See how we build &darr;</a>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 140px 32px 100px;
    position: relative;
  }

  .hero__eyebrow {
    margin-bottom: 20px;
    color: var(--color-text-dim);
  }

  .hero__heading {
    font-family: var(--font-display);
    font-size: clamp(36px, 6.5vw, 72px);
    font-weight: 400;
    line-height: 1.12;
    letter-spacing: -0.03em;
    margin-bottom: 32px;
    max-width: 900px;
  }

  .hero__heading .text-accent {
    font-style: italic;
    display: block;
    margin-top: 4px;
    font-size: clamp(22px, 4vw, 46px);
  }

  .hero__body {
    font-size: 18px;
    line-height: 1.7;
    color: var(--color-text-muted);
    max-width: 560px;
    margin-bottom: 20px;
  }

  .hero__proof {
    color: var(--color-text-dim);
    margin-bottom: 40px;
  }

  .hero__actions {
    display: flex;
    gap: 24px;
    flex-wrap: wrap;
    align-items: center;
  }

  .hero__link {
    font-family: var(--font-mono);
    font-size: 12px;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    color: var(--color-text-dim);
    padding: 15px 0;
    transition: color 0.3s ease;
    position: relative;
  }

  .hero__link::after {
    content: '';
    position: absolute;
    bottom: 12px;
    left: 0;
    width: 0;
    height: 1px;
    background: var(--color-text);
    transition: width 0.3s ease;
  }

  .hero__link:hover {
    color: var(--color-text);
  }

  .hero__link:hover::after {
    width: 100%;
  }

  @media (max-width: 768px) {
    .hero {
      padding: 120px 20px 80px;
    }
  }
</style>
