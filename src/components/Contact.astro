<section class="contact" id="contact">
  <div class="container--narrow">
    <div data-fade>
      <h2 class="contact__heading">Tell us what you're building.</h2>
      <p class="contact__subheading">
        Book a 30-minute call. If there's a fit, we'll map your product
        and show you how fast we can ship it.
      </p>
      <p class="contact__capacity">We take on 3&ndash;4 engagements at a time so every client gets founder-level attention.</p>
    </div>
    <div class="contact__embed" data-fade data-fade-delay="0.1">
      <div id="cal-embed"></div>
    </div>
  </div>
</section>

<script>
  (function (C: any, A: string, L: string) {
    let p = function (a: any, ar: any) { a.q.push(ar); };
    let d = C.document;
    C.Cal = C.Cal || function () {
      let cal = C.Cal;
      let ar = arguments;
      if (!cal.loaded) {
        cal.ns = {};
        cal.q = cal.q || [];
        d.head.appendChild(d.createElement("script")).src = A;
        cal.loaded = true;
      }
      if (ar[0] === L) {
        const api = function () { p(api, arguments); };
        const namespace = ar[1];
        api.q = api.q || [];
        typeof namespace === "string" ? (cal.ns[namespace] = api) && p(api, ar) : p(cal, ar);
        return;
      }
      p(cal, ar);
    };
  })(window, "https://app.cal.com/embed/embed.js", "init");

  (window as any).Cal("init", { origin: "https://cal.com" });
  (window as any).Cal("inline", {
    elementOrSelector: "#cal-embed",
    calLink: "alecw",
    layout: "month_view",
  });
  (window as any).Cal("ui", {
    theme: "dark",
    styles: {
      branding: { brandColor: "#b8a88a" },
    },
    hideEventTypeDetails: false,
    layout: "month_view",
  });
</script>

<style>
  .contact {
    padding: 120px 24px 80px;
  }

  .contact__heading {
    font-size: clamp(32px, 5vw, 48px);
    font-weight: 700;
    letter-spacing: -0.035em;
    margin-bottom: 16px;
    line-height: 1.1;
    text-align: center;
  }

  .contact__subheading {
    font-size: 16px;
    color: var(--color-text-dim);
    text-align: center;
    margin-bottom: 12px;
    line-height: 1.6;
  }

  .contact__capacity {
    font-family: var(--font-mono);
    font-size: 12px;
    letter-spacing: 0.03em;
    color: var(--color-accent);
    text-align: center;
    margin-bottom: 48px;
  }

  .contact__embed {
    min-height: 400px;
    border-radius: 8px;
    overflow: hidden;
  }
</style>
